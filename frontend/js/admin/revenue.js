async function loadRevenue() { const content = document.getElementById('adminContent'); content.innerHTML = '<h2>Revenue</h2><p class="lede">Paystack payment records will appear here.</p><div class="panel"><div class="empty">Loading revenue…</div></div>'; try { const data = await adminApi.get('/revenue'); const money = new Intl.NumberFormat('en-NG',{style:'currency',currency:'NGN'}).format(data.total); content.innerHTML = `<h2>Revenue</h2><p class="lede">Paystack integration is ready for transaction records.</p><div class="stats"><div class="stat-card"><div class="stat-label">Recorded payment revenue</div><div class="stat-value">${money}</div><div class="stat-note">${data.transactions.length} transaction(s)</div></div></div><div class="notice">Paystack sync is currently a stub; vouchers remain available independently.</div><div class="panel table-wrap" style="margin-top:20px"><table><thead><tr><th>Reference</th><th>User</th><th>Package</th><th>Amount</th><th>Status</th><th>Date</th></tr></thead><tbody>${rows(data.transactions, (t) => `<tr><td>${esc(t.paystack_reference || '—')}</td><td>${esc(t.username || '—')}</td><td>${esc(t.package_name || '—')}</td><td>${new Intl.NumberFormat('en-NG',{style:'currency',currency:'NGN'}).format(t.amount || 0)}</td><td><span class="badge ${esc(t.status)}">${esc(t.status)}</span></td><td>${fmtDate(t.created_at)}</td></tr>`)}</tbody></table></div>`; } catch(e) { content.innerHTML=`<div class="notice">${esc(e.message)}</div>`; } }
if (document.body.dataset.page === 'revenue') loadRevenue();
